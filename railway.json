{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt update && apt install -y maven && mvn -version && mvn -f backend/pom.xml clean package"
    },
  "deploy": {
    "startCommand": "mvn -f backend/pom.xml tomcat7:run"
    }
}