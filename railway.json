{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "sudo apt update && sudo apt install -y maven && mvn -version && mvn -f backend/pom.xml clean package || true"
    },
  "deploy": {
    "startCommand": "mvn -f backend/pom.xml tomcat7:run"
    }
}